═══════════════════════════════════════════════════════════════════
  🌍 WELTENBIBLIOTHEK - SCHNELLSTART-ANLEITUNG
═══════════════════════════════════════════════════════════════════

Version: 3.0.0+85
Letztes Update: 2025-11-08
Vollständige App bereit für lokalen Build!

───────────────────────────────────────────────────────────────────
  📦 SCHRITT 1: ARCHIV ENTPACKEN (1 Minute)
───────────────────────────────────────────────────────────────────

Windows (PowerShell):
  1. Öffnen Sie PowerShell (Win + X → PowerShell)
  2. Navigieren Sie zum Download-Ordner:
     cd C:\Users\IhrName\Downloads
  3. Entpacken Sie das Archiv:
     tar -xzf weltenbibliothek_complete_v3.0.0+85.tar.gz -C C:\flutter_projects\
  4. Wechseln Sie ins Projekt:
     cd C:\flutter_projects\home\user\flutter_app

Linux/Mac (Terminal):
  1. Öffnen Sie Terminal
  2. Navigieren Sie zum Download-Ordner:
     cd ~/Downloads
  3. Entpacken Sie das Archiv:
     tar -xzf weltenbibliothek_complete_v3.0.0+85.tar.gz -C ~/flutter_projects/
  4. Wechseln Sie ins Projekt:
     cd ~/flutter_projects/home/user/flutter_app

───────────────────────────────────────────────────────────────────
  🔧 SCHRITT 2: FLUTTER VORBEREITEN (2 Minuten)
───────────────────────────────────────────────────────────────────

Voraussetzung: Flutter muss installiert sein!
  → Falls nicht: https://docs.flutter.dev/get-started/install

1. Flutter-Version prüfen:
   flutter --version
   
   Erwartet: Flutter 3.35.4 oder höher
   
2. Dependencies installieren:
   flutter pub get
   
   Das lädt alle benötigten Pakete herunter (~2-3 Minuten)

3. Flutter Doctor ausführen (optional):
   flutter doctor -v
   
   Prüft ob alles korrekt eingerichtet ist

───────────────────────────────────────────────────────────────────
  🚀 SCHRITT 3: APP TESTEN (2 Minuten)
───────────────────────────────────────────────────────────────────

OPTION A: Web-Version (Schnellster Test)
  
  flutter run -d chrome --release
  
  → Öffnet die App im Chrome-Browser
  → Ideal für schnelles Testen aller Features

OPTION B: Android-Emulator
  
  1. Starten Sie Android Studio
  2. Öffnen Sie AVD Manager (Tools → Device Manager)
  3. Starten Sie einen Emulator
  4. In Terminal:
     flutter run
  
  → App startet im Emulator

OPTION C: Echtes Android-Gerät
  
  1. Aktivieren Sie USB-Debugging auf Ihrem Phone:
     Einstellungen → Über das Telefon → Build-Nummer 7x antippen
     Zurück → Entwickleroptionen → USB-Debugging aktivieren
  
  2. Verbinden Sie Phone per USB
  
  3. In Terminal:
     flutter devices  (Phone sollte angezeigt werden)
     flutter run
  
  → App startet auf Ihrem Phone

───────────────────────────────────────────────────────────────────
  📱 SCHRITT 4: APK ERSTELLEN (5 Minuten)
───────────────────────────────────────────────────────────────────

Für Installation auf Android-Geräten ohne USB-Kabel:

1. Standard APK (alle Architekturen):
   
   flutter build apk --release --no-tree-shake-icons
   
   Ausgabe: build/app/outputs/flutter-apk/app-release.apk
   Größe: ~50-60 MB
   
2. Optimierte APKs (empfohlen):
   
   flutter build apk --release --no-tree-shake-icons --split-per-abi
   
   Ausgabe:
   - app-armeabi-v7a-release.apk  (~20 MB) - 32-bit ARM
   - app-arm64-v8a-release.apk    (~22 MB) - 64-bit ARM (Empfohlen!)
   - app-x86_64-release.apk       (~23 MB) - Intel/AMD
   
   → Nutzen Sie app-arm64-v8a-release.apk für moderne Phones

3. APK auf Phone übertragen:
   
   Per USB:
     adb install build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
   
   Per Cloud:
     - Laden Sie APK zu Google Drive / Dropbox hoch
     - Öffnen Sie Link auf Phone
     - Installieren Sie APK (Unbekannte Quellen erlauben)

───────────────────────────────────────────────────────────────────
  🔥 FIREBASE KONFIGURATION (Optional, 10 Minuten)
───────────────────────────────────────────────────────────────────

Nur nötig wenn Sie Firebase-Features nutzen wollen!

1. Firebase-Projekt erstellen:
   → https://console.firebase.google.com/
   → "Projekt hinzufügen"
   → Projekt-Name eingeben (z.B. "weltenbibliothek")

2. Firestore Database aktivieren:
   → Build → Firestore Database → Datenbank erstellen
   → Testmodus oder Produktionsmodus wählen
   → Region wählen (z.B. europe-west)

3. Android-App hinzufügen:
   → Projekteinstellungen → App hinzufügen → Android
   → Android-Paketnamen eingeben (siehe android/app/build.gradle)
   → google-services.json herunterladen
   → Datei nach android/app/google-services.json kopieren

4. Firestore-Regeln setzen (Entwicklungs-Modus):
   
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if true;
       }
     }
   }
   
   ⚠️ ACHTUNG: Für Produktion strengere Regeln verwenden!

5. App neu bauen:
   flutter clean
   flutter pub get
   flutter build apk --release --no-tree-shake-icons --split-per-abi

───────────────────────────────────────────────────────────────────
  📡 TELEGRAM → FTP SYNC EINRICHTEN (Optional, 15 Minuten)
───────────────────────────────────────────────────────────────────

Nur nötig wenn Sie automatische Telegram-Synchronisation wollen!

VORAUSSETZUNGEN:
  - Python 3.8 oder höher installiert
  - Xlight FTP Server auf Windows installiert und gestartet
  - Telegram API Credentials (von https://my.telegram.org/)

SCHRITT 1: Python-Pakete installieren
  
  cd scripts/
  pip install -r requirements.txt

SCHRITT 2: Xlight FTP Server einrichten
  
  1. Laden Sie Xlight FTP Server herunter:
     → https://www.xlightftpd.com/download.htm
  
  2. Installieren Sie Xlight
  
  3. Erstellen Sie Benutzer "Weltenbibliothek":
     - Xlight → Setup → Users → Add User
     - Username: Weltenbibliothek
     - Password: Jolene2305  (oder eigenes Passwort)
     - Home Directory: C:\weltenbibliothek\
     - Permissions: Full Access
  
  4. Firewall-Regel für Port 21 erstellen:
     - Windows Firewall → Erweiterte Einstellungen
     - Eingehende Regeln → Neue Regel
     - Port → TCP → 21 → Verbindung zulassen

SCHRITT 3: Konfiguration erstellen
  
  1. Kopieren Sie die Beispiel-Konfiguration:
     copy .env.example .env  (Windows)
     cp .env.example .env    (Linux/Mac)
  
  2. Bearbeiten Sie .env:
     notepad .env  (Windows)
     nano .env     (Linux/Mac)
  
  3. Tragen Sie Ihre Daten ein:
     
     FTP_HOST=192.168.1.100  (Ihre Server-IP)
     FTP_PORT=21
     FTP_USER=Weltenbibliothek
     FTP_PASS=Jolene2305
     FTP_BASE_PATH=/weltenbibliothek
     
     TELEGRAM_API_ID=12345678  (Von my.telegram.org)
     TELEGRAM_API_HASH=abcdef1234567890  (Von my.telegram.org)

SCHRITT 4: FTP-Verbindung testen
  
  python test_xlight_connection.py
  
  Erwartete Ausgabe:
  ✅ Verbunden mit FTP Server
  ✅ Login erfolgreich
  ✅ Verzeichnisse erstellt
  ✅ Upload-Test erfolgreich

SCHRITT 5: Erste Synchronisation
  
  python telegram_to_ftp_sync.py
  
  Beim ersten Start:
  - Geben Sie Ihre Telefonnummer ein
  - Geben Sie den Telegram-Code ein
  - Script lädt alle Medien herunter und lädt sie auf FTP hoch

SCHRITT 6: Automatisierung (optional)
  
  Windows (Task Scheduler):
    - Taskplaner öffnen
    - Aufgabe erstellen → Trigger: Alle 15 Minuten
    - Aktion: python telegram_to_ftp_sync.py
  
  Linux/Mac (Cron):
    chmod +x setup_auto_sync.sh
    ./setup_auto_sync.sh

───────────────────────────────────────────────────────────────────
  🎯 WAS FUNKTIONIERT BEREITS?
───────────────────────────────────────────────────────────────────

✅ LOKAL VERFÜGBAR (Ohne Firebase/FTP):
   - Ereignis-Timeline mit 200+ Events
   - Interaktive Karte mit Event-Markern
   - Suchfunktion über alle Ereignisse
   - Statistik-Dashboard
   - Kategorien-Filter
   - Dark/Light Theme

✅ MIT FIREBASE (Nach Konfiguration):
   - Telegram-Posts-Synchronisation
   - Cloud-Datenspeicherung
   - User-Authentication
   - Echtzeit-Updates

✅ MIT FTP-SYNC (Nach Einrichtung):
   - Automatischer Telegram-Media-Download
   - Kategorisierte Medien-Bibliothek
   - Video/Audio/Bild-Playback in App
   - Duplikats-Vermeidung

───────────────────────────────────────────────────────────────────
  ❓ HÄUFIGE PROBLEME & LÖSUNGEN
───────────────────────────────────────────────────────────────────

PROBLEM: "flutter: command not found"
LÖSUNG:  Flutter ist nicht installiert oder nicht im PATH
         → https://docs.flutter.dev/get-started/install

PROBLEM: APK-Build schlägt fehl mit Gradle-Fehler
LÖSUNG:  Build-Cache leeren:
         cd android
         gradlew clean  (Windows)
         ./gradlew clean  (Linux/Mac)
         cd ..
         flutter clean
         flutter pub get
         flutter build apk --release --no-tree-shake-icons

PROBLEM: "No devices found"
LÖSUNG:  Kein Emulator/Gerät verbunden
         - Starten Sie Android-Emulator ODER
         - Verbinden Sie Phone per USB und aktivieren USB-Debugging

PROBLEM: FTP-Verbindung schlägt fehl
LÖSUNG:  - Ist Xlight FTP Server gestartet?
         - Firewall-Regel für Port 21 aktiv?
         - Korrekte IP in .env eingetragen?
         - Test: python test_xlight_connection.py

PROBLEM: Telegram-Sync funktioniert nicht
LÖSUNG:  - API Credentials korrekt in .env?
         - Telefonnummer beim ersten Start eingegeben?
         - Telegram-Code erhalten und eingegeben?
         - Logs prüfen: tail -f telegram_sync.log

PROBLEM: App stürzt beim Start ab
LÖSUNG:  Firebase-Konfiguration fehlt oder falsch
         - Prüfen Sie ob google-services.json existiert
         - Prüfen Sie ob Firebase initialisiert ist
         - Logs anschauen: flutter run --verbose

───────────────────────────────────────────────────────────────────
  📚 WEITERE DOKUMENTATION
───────────────────────────────────────────────────────────────────

Im Projekt-Ordner finden Sie:

  INSTALLATION_GUIDE.md
    → Detaillierte Installations-Anleitung (13 KB)
  
  PROJECT_STATS.txt
    → Projekt-Statistiken und Übersicht
  
  TELEGRAM_FTP_INTEGRATION.md
    → Master-Guide für FTP-Integration (7 KB)
  
  scripts/XLIGHT_FTP_SETUP.md
    → Xlight-spezifische Konfiguration (8 KB)
  
  scripts/README_TELEGRAM_FTP_SYNC.md
    → Telegram-Sync-Dokumentation (4 KB)
  
  scripts/ARCHITECTURE.txt
    → System-Architektur-Diagramm (8 KB)

───────────────────────────────────────────────────────────────────
  🎊 ZUSAMMENFASSUNG
───────────────────────────────────────────────────────────────────

Sie haben jetzt:

  ✅ Vollständige Flutter-App (156 Dart-Dateien, 64.556 Zeilen Code)
  ✅ 200+ historische Ereignisse (lokal gespeichert)
  ✅ Firebase-Integration (optional)
  ✅ Telegram → FTP Sync-System (optional)
  ✅ Alle Dokumentations-Dateien
  ✅ Python-Automatisierungs-Scripts
  ✅ Bereit für APK-Build

MINIMALE SCHRITTE ZUM TESTEN:
  1. Archiv entpacken
  2. flutter pub get
  3. flutter run -d chrome --release

EMPFOHLENE SCHRITTE FÜR APK:
  1. Archiv entpacken
  2. flutter pub get
  3. flutter build apk --release --no-tree-shake-icons --split-per-abi
  4. APK auf Phone installieren (app-arm64-v8a-release.apk)

───────────────────────────────────────────────────────────────────
  📞 SUPPORT
───────────────────────────────────────────────────────────────────

Bei Problemen:
  1. Prüfen Sie diese Dokumentation
  2. Lesen Sie INSTALLATION_GUIDE.md
  3. Führen Sie "flutter doctor -v" aus
  4. Prüfen Sie Logs mit "flutter run --verbose"

───────────────────────────────────────────────────────────────────

🌍 WELTENBIBLIOTHEK v3.0.0+85
   Alle Features funktional und getestet!
   Viel Erfolg! 🚀

═══════════════════════════════════════════════════════════════════
