╔═══════════════════════════════════════════════════════════════════════════╗
║                  TELEGRAM → XLIGHT FTP → FLUTTER APP                       ║
║                         SYSTEM ARCHITEKTUR                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                         PHASE 1: TELEGRAM SOURCE                         │
└─────────────────────────────────────────────────────────────────────────┘

    📱 Telegram Channel (@your_channel)
    │
    │  Medien-Posts mit Hashtags:
    │  • Videos (#technologie, #mystik)
    │  • Audios (#kosmos, #paranormal)
    │  • Bilder (#verboten)
    │  • PDFs  
    │
    ▼
    
    ┌────────────────────────────┐
    │   Pyrogram Client API      │
    │   • API_ID, API_HASH       │
    │   • Session Management     │
    └────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    PHASE 2: PYTHON SYNC SCRIPT                           │
└─────────────────────────────────────────────────────────────────────────┘

    🐍 telegram_to_ftp_sync.py
    │
    ├─ 1. Verbinde mit Telegram
    │     └─> Hole neueste 100 Nachrichten
    │
    ├─ 2. Filtere Medien-Nachrichten
    │     └─> Videos, Audios, Bilder, PDFs
    │
    ├─ 3. Kategorisierung
    │     ├─> #technologie  → tech_mysteries
    │     ├─> #mystik       → mysticism
    │     ├─> #kosmos       → cosmos
    │     ├─> #verboten     → forbidden
    │     └─> #paranormal   → paranormal
    │
    ├─ 4. Download zu lokalem Ordner
    │     └─> /downloads/filename.ext
    │
    ├─ 5. Upload zu Xlight FTP
    │     └─> ftp://server/weltenbibliothek/videos/
    │
    └─ 6. Metadaten-Tracking
          └─> sync_metadata.json

┌─────────────────────────────────────────────────────────────────────────┐
│                      PHASE 3: XLIGHT FTP SERVER                          │
└─────────────────────────────────────────────────────────────────────────┘

    🖥️ Xlight FTP Server
    │
    ├── User: Weltenbibliothek
    ├── Pass: Jolene2305
    ├── Port: 21 (FTP) / 990 (FTPS)
    │
    └── Ordnerstruktur:
        /weltenbibliothek/
        ├── videos/        ← 🎥 Video-Dateien
        ├── audios/        ← 🎵 Audio/Podcast
        ├── images/        ← 🖼️ Bilder
        ├── pdfs/          ← 📄 PDF-Dokumente
        └── documents/     ← 📋 Sonstige Dateien

┌─────────────────────────────────────────────────────────────────────────┐
│                    PHASE 4: HTTP PROXY (OPTIONAL)                        │
└─────────────────────────────────────────────────────────────────────────┘

    🌐 IIS / Nginx / Apache
    │
    ├─ Virtual Directory Mapping:
    │  /media/ → C:\ftp-data\weltenbibliothek\
    │
    └─ URLs werden zu:
       https://your-domain.com/media/videos/video.mp4

┌─────────────────────────────────────────────────────────────────────────┐
│                       PHASE 5: FLUTTER APP                               │
└─────────────────────────────────────────────────────────────────────────┘

    📱 Flutter App (Weltenbibliothek)
    │
    ├── FTPMediaService
    │   ├─ getVideoUrl()   → Video URLs
    │   ├─ getAudioUrl()   → Audio URLs
    │   ├─ getImageUrl()   → Bild URLs
    │   └─ getPDFUrl()     → PDF URLs
    │
    ├── Video Player Widget
    │   └─> VideoPlayerController.network(url)
    │
    ├── Audio Player Widget
    │   └─> AudioPlayer(url)
    │
    └── Image Widget
        └─> Image.network(url)

┌─────────────────────────────────────────────────────────────────────────┐
│                         AUTOMATISIERUNG                                  │
└─────────────────────────────────────────────────────────────────────────┘

    ⏰ Cron Job (Linux/Mac) / Task Scheduler (Windows)
    │
    ├─ Alle 15 Minuten:
    │  */15 * * * * cd /path/to/scripts && python3 telegram_to_ftp_sync.py
    │
    ├─ Oder stündlich:
    │  0 * * * * cd /path/to/scripts && python3 telegram_to_ftp_sync.py
    │
    └─ Oder täglich um 3:00 Uhr:
       0 3 * * * cd /path/to/scripts && python3 telegram_to_ftp_sync.py

┌─────────────────────────────────────────────────────────────────────────┐
│                         DATENFLUSS-BEISPIEL                              │
└─────────────────────────────────────────────────────────────────────────┘

  1️⃣  Telegram Post:
      "Mysteriöse Pyramiden-Technologie! #technologie"
      [video.mp4 attached]

  2️⃣  Python Script:
      • Erkennt Video
      • Kategorisiert als "tech_mysteries"
      • Lädt herunter nach downloads/video.mp4

  3️⃣  FTP Upload:
      • Verbindet mit Xlight Server
      • Upload nach /weltenbibliothek/videos/video.mp4
      • URL: ftp://server/weltenbibliothek/videos/video.mp4

  4️⃣  HTTP Proxy (optional):
      • Mappe FTP zu HTTP
      • URL: https://domain.com/media/videos/video.mp4

  5️⃣  Flutter App:
      FTPMediaService().getVideoUrl('video.mp4')
      → Lädt und spielt Video ab

┌─────────────────────────────────────────────────────────────────────────┐
│                      SICHERHEITS-ARCHITEKTUR                             │
└─────────────────────────────────────────────────────────────────────────┘

    🔐 Authentifizierung
    ├── Telegram: API_ID + API_HASH + Session
    ├── FTP: Username + Password
    └── HTTP: Optional Basic Auth / OAuth

    🔥 Firewall
    ├── Port 21  (FTP) - Eingehend erlaubt
    ├── Port 990 (FTPS) - Optional für verschlüsselte Verbindung
    └── Port 80/443 (HTTP/HTTPS) - Für Web-Zugriff

    🔒 Verschlüsselung
    ├── TLS/SSL für FTPS (Port 990)
    ├── HTTPS für Web-Zugriff
    └── Telegram API automatisch verschlüsselt

┌─────────────────────────────────────────────────────────────────────────┐
│                      MONITORING & LOGGING                                │
└─────────────────────────────────────────────────────────────────────────┘

    📊 Logs
    ├── sync.log                 ← Python Script Logs
    ├── sync_metadata.json       ← Sync-Status & Tracking
    ├── Xlight Server Logs       ← FTP Server Activity
    └── Flutter App Debug Logs   ← Media Loading Status

    📈 Metriken
    ├── Anzahl synchronisierter Dateien
    ├── Fehlerrate
    ├── Upload-Geschwindigkeit
    └── Storage-Nutzung

┌─────────────────────────────────────────────────────────────────────────┐
│                         FEHLERBEHANDLUNG                                 │
└─────────────────────────────────────────────────────────────────────────┘

    🔄 Retry-Logik
    ├── FTP Connection Timeout    → 3 Versuche
    ├── Telegram API Rate Limit   → Exponential Backoff
    └── Download Fehler           → Skip & Continue

    ⚠️ Error Recovery
    ├── Duplikat-Erkennung        → sync_metadata.json
    ├── Partial Upload            → Automatische Bereinigung
    └── Connection Lost           → Neuverbindung bei nächstem Cron

╔═══════════════════════════════════════════════════════════════════════════╗
║                         TECHNOLOGIE-STACK                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

    Backend:
    • Python 3.8+
    • Pyrogram 2.0 (Telegram Client)
    • ftplib (Standard Library)
    • python-dotenv (Environment Variables)

    FTP Server:
    • Xlight FTP Server
    • User: Weltenbibliothek
    • Pass: Jolene2305

    Frontend:
    • Flutter 3.35.4
    • Dart 3.9.2
    • video_player package
    • http package

    Optional:
    • IIS / Nginx / Apache (HTTP Proxy)
    • Firebase (Metadaten-Storage)
    • Cloudflare (CDN)

╔═══════════════════════════════════════════════════════════════════════════╗
║                       DEPLOYMENT CHECKLISTE                               ║
╚═══════════════════════════════════════════════════════════════════════════╝

    ✅ Xlight FTP Server installiert und läuft
    ✅ Benutzer "Weltenbibliothek" erstellt
    ✅ Ordnerstruktur erstellt (/weltenbibliothek/videos/, etc.)
    ✅ Firewall Port 21 geöffnet
    ✅ Python 3 installiert
    ✅ Pyrogram & Dependencies installiert
    ✅ .env Datei konfiguriert
    ✅ Telegram API Keys erstellt
    ✅ FTP-Verbindung getestet
    ✅ Erster Sync erfolgreich
    ✅ Cron Job eingerichtet
    ✅ Flutter App FTP Service integriert
    ✅ Optional: HTTP Proxy konfiguriert
    ✅ Optional: FTPS/HTTPS aktiviert

╔═══════════════════════════════════════════════════════════════════════════╗
║                            FERTIG! 🎉                                     ║
╚═══════════════════════════════════════════════════════════════════════════╝
